- provide :submit_button, 'Обновить'
.container.content-wrapper.buffer-both
  .column-1-2
    %h1.title-medium
      Править профиль
    = render 'form'
  .column-1-2
    .preview
      %h1.title-medium.text-center
        Предпросмотр
      #preview-upper.user-header.text-center
        %h1#preview-name.title-medium
          = @user.name
        #preview-border.user-image-div
          = image_tag user_avatar(@user), class: 'user-image', alt: '[Фото]', id: 'preview-avatar'
        %h2#preview-username.title-small.buffer-both
          @#{@user.username}
        .buffer-both
          Редактировать профиль
        .container.text-center
          .button.button-light
            ¿Есть вопрос?
      #preview-lower.user-details
        .user-statistics.container.text-center
          .column-1-3
            = t(:question, count: 21)
          .column-1-3
            = t(:unanswered, count: 20)
          .column-1-3
            = t(:answer, count: 1)
